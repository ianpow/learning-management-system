{
  "version": 2,
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "regions": ["lhr1"],
  "functions": {
    "app/api/courses/route": {
      "maxDuration": 60
    }
  }
}