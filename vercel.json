{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next"
      }
    ],

    "buildCommand": "prisma generate && prisma migrate deploy && next build",
    "regions": ["lhr1"],
    "functions": {
      "api/*": {
        "memory": 1024,
        "maxDuration": 10
      }
    }
  }